pipeline {
options { buildDiscarder(logRotator (numToKeepStr: '60')) }
agent {label 'Docker-Slavel-jnkrck-vdcn004'}

stages {
  
stage('CheckoutGit') {
steps {
  I checkout([$class: 'GitSCM',branches: [[name: 'release']],
doGenerateSubmoduleConfigurations: false,extensions: [],submoduleCfg: [],userRemoteConfigs: [[credentialsId: 'svcsscns_github_token'
      }
url: 'org-2033@github.wsgc.com: Logistics-WMOS/sdnfixpack.git']]])

stage('SDNfixpack Automation'){
steps {
}
dir('ansible') {
wrap([$class: 'AnsiColorBuildWrapper', 'colorMapName': 'XTerm']) {
script {String[] SDNs = params.SDNs.split(",");
for (String SDN: SDNs) {
sh "pwd"
sh "export ANSIBLE_FORCE_COLOR=true; ansible-playbook |
download_SDN_from_artifactory.yml --user=svcswmosnpd --extra-vars sdn_version=${SDN}
-extra-vars is_this_0B2=${params.OB2_or_PH1} -extra-vars workspace=${WORKSPACE}"
}
}
}
}
}
}
