---
- name: Execute local script on server
  hosts: localhost
  # become: yes

  tasks:
    - name: Execute bash script
      ansible.builtin.script: getIP.sh
      register: output

    - name: Display output
      ansible.builtin.debug:
        msg: "{{ output.stdout }}"